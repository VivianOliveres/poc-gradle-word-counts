plugins {
    id 'application'
    id 'com.kensai.poc.scala-common-conventions'
}

dependencies {
    compile group: 'org.apache.spark', name: "spark-sql_${scalaMajorVersion}", version: '3.1.0'
}

application {
    mainClass = 'com.kensai.poc.spark.SparkApp'
}
